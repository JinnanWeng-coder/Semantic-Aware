# Data outputs

Training and evaluation scripts write generated `.npy` result arrays here.

The generated arrays are intentionally ignored by git. Keep this directory in
the repository so fresh clones have a conventional output location.
